# Windows + Wind composites — placeholder

The 5 Windows+Wind composite assets (`Windows+Wind-icon_01_RGB` through `_05_RGB`) ship from Ahoy as `.ai` and `.pdf` only. No SVG or PNG variants were delivered in the handoff.

Two options to populate this folder:

1. **Export PNG/SVG from source PDFs** at the design layer (Figma/Illustrator) and drop into this folder. Easiest, preserves Ahoy's exact composition.
2. **Compose at runtime** by layering individual `_brand/graphics/wind/wind.svg` and `_brand/graphics/windows/window-NN.svg`. Recommended — preserves runtime regional-colour theming via SVG `fill` attributes.

Flag for design-director attention in the next session.
