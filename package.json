{
  "name": "breath-city-design-system",
  "type": "module",
  "version": "1.0.0",
  "description": "Design system tokens for Breathe Cities — compiles to CSS custom properties and Tailwind theme",
  "scripts": {
    "build": "style-dictionary build --config style-dictionary.config.js",
    "watch": "nodemon --watch tokens --ext json --exec 'npm run build'"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "style-dictionary": "^4.3.0"
  }
}
